name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  repository-integrity:
    name: Repository integrity
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Validate local Markdown links
        run: python3 scripts/check_markdown_links.py

      - name: Validate documentation inventory
        run: python3 scripts/check_documentation_inventory.py

      - name: Scan committed text for credentials
        run: python3 scripts/check_repository_secrets.py

      - name: Test repository scripts
        run: python3 -m unittest discover -s scripts/tests -p 'test_*.py'

  build-test:
    name: ${{ matrix.os }} / .NET 9
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 9.0.x
          cache: true
          cache-dependency-path: |
            **/*.csproj
            Directory.Packages.props

      - name: Restore
        run: dotnet restore ChronoDesk.sln

      - name: Verify formatting
        run: dotnet format ChronoDesk.sln --verify-no-changes --no-restore

      - name: Build
        run: dotnet build ChronoDesk.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ChronoDesk.sln --configuration Release --no-build --collect:"XPlat Code Coverage" --results-directory TestResults

      - name: Check vulnerable packages
        shell: pwsh
        run: |
          $output = dotnet list ChronoDesk.sln package --vulnerable --include-transitive 2>&1 | Out-String
          $output | Write-Host
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          if ($output -match "has the following vulnerable packages") {
            Write-Error "Vulnerable NuGet dependencies were reported."
            exit 1
          }

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results-${{ matrix.os }}
          path: TestResults
          if-no-files-found: ignore
          retention-days: 14
